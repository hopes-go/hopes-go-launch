# Hope's & Go Simple App

A simple, Railway-ready Hope's & Go app prototype for customer requests,
driver availability, delivery order details, and Stripe checkout planning.

## Run locally

```sh
npm install
npm start
```

Open `http://localhost:3000`.

## Deploy on Railway

Connect the `hopes-go-launch` GitHub repository. Railway will detect the Node app and run `npm start` automatically.
